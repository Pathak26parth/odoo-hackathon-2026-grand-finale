
/**
 * Authentication Controller
 * Unified module supporting both named imports and default instance export.
 * Fully compatible with both require('./auth.controller') and require('./authController').
 */
const authController = require('./authController');

module.exports = authController;

